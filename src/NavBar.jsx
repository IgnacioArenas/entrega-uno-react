const NavBar = ({campeones, runas, objetos}) => {
    return (
        <div style={{backgroundColor: 'darkred'}}>
            {campeones}, {runas}, {objetos}
        </div>
    )
}

export default NavBar;